const createAccount = require('./create-account')

test('should create a new firebase entry with user id', function () {

})

test('should populate the new firebase entry with $100', function () {

})
